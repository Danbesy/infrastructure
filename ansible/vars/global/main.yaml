project_base_dir: "{{ ansible_facts['env']['HOME'] }}/infrastructure"

ssl_certs:
  - domain: danbesy.com
    provider: cloudflare
    cert_name: cert.pem
    key_name: key.pem
  - domain: danbesy.com
    provider: letsencrypt
    cert_name: fullchain.pem
    key_name: key.pem
  - domain: danbesy.lol
    provider: cloudflare
    cert_name: cert.pem
    key_name: key.pem